version: "2"

linters:
  default: standard
  enable:
    - gosec
    - errcheck
    - misspell
    - ineffassign
  settings:
    gosec:
      excludes:
        - G304  # file path from variable (config files)
        - G703  # path traversal in config lookup (intentional)

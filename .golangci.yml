version: "2"

linters:
  default: standard
  enable:
    - gosec
    - errcheck
    - misspell
    - ineffassign
  settings:
    gosec:
      excludes:
        - G304  # file path from variable (expected — we read config files)

issues:
  exclude-dirs:
    - vendor

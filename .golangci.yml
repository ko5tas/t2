linters:
  enable:
    - gosec
    - errcheck
    - staticcheck
    - govet
    - ineffassign
    - unused
    - misspell

linters-settings:
  gosec:
    excludes:
      - G304  # file path from variable (expected — we read config files)

issues:
  exclude-dirs:
    - vendor
